fastapi>=0.115
uvicorn[standard]>=0.30
asyncpg>=0.30
aiokafka>=0.10
# Kafka compression codecs. gzip is stdlib; cramjam covers snappy + lz4 + zstd
# (aiokafka 0.14's has_snappy()/has_lz4() are literally `cramjam is not None`).
# Without it aiokafka raises UnsupportedCodecError *inside* the fetch and the
# worker crash-loops on the poison offset — e.g. a message written by
# `rpk produce`, whose default compression is snappy. Pure prebuilt wheel,
# no system libs needed.
cramjam>=2.8
arq>=0.26
redis>=5.0
pydantic>=2.0
pydantic-settings>=2.0
email-validator>=2.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.9
httpx>=0.27
anthropic>=0.34
alembic>=1.13
sqlalchemy[asyncio]>=2.0
psycopg2-binary>=2.9
slowapi>=0.1.9
cryptography>=43.0
croniter>=2.0
beautifulsoup4>=4.12
# Stealth scraping (Naukri / ProductHunt fallback paths).
# setuptools is pinned because Python 3.12 removed distutils from stdlib and
# undetected-chromedriver 3.5.x still imports it; setuptools ships a vendored
# distutils that gets shimmed back in.
setuptools>=68
selenium>=4.20
undetected-chromedriver>=3.5
# Dev / test
ruff>=0.11
pytest>=8.0,<9.0
pytest-asyncio>=0.25
