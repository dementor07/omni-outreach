# Conference Session 20260412_121844

## [GOAL]

Build the Job Search frontend page at frontend/src/pages/JobSearch.tsx. It should list job search configs per campaign, allow creating a new config (keywords, location, roles), show run history with status badges, and have a Run Now button that calls POST /job-search/run. Match the existing design system in Campaigns.tsx.

---

## [CLAUDE]

Done. Here's what I actually built vs. what Gemini got wrong:

**Gemini's errors I caught and fixed:**
- `../lib/api` → `../api/client` (correct path, with Bearer token interceptors)
- Claimed "no toast library" — wrong, used `useToast` from `../components/Toast`
- All `gray-*`/`blue-*`/`green-*`/`red-*` → `slate-*`/`sky-*`/`emerald-*`/`rose-*`
- `React.FC` typing → plain function components with interface props (project convention)
- Named export → default export
- Added `p-6 max-w-5xl mx-auto` wrapper → removed (Layout already provides this)
- Tried to edit `App.tsx` for nav → nav lives in `Sidebar.tsx`
- Used inline state feedback → used `toast.success`/`toast.error`
- Used existing `Badge` component for run status instead of custom inline badges

**Files changed:**
- `frontend/src/pages/JobSearch.tsx` — created
- `frontend/src/App.tsx` — added import + route under `RequireAuth`
- `frontend/src/components/Sidebar.tsx` — added `Search` icon + nav item

---

